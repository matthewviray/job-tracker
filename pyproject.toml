[project]
name = "job-tracker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Matthew Viray", email = "matvir437@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "click>=8.4.1",
    "rich>=15.0.0",
]

[project.scripts]
job-tracker = "job_tracker:main"

[build-system]
requires = ["uv_build>=0.11.8,<0.12.0"]
build-backend = "uv_build"
